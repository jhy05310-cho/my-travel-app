# 05 — 30일 도입 로드맵

- 시작일: 2026-08-11 (월)
- 종료일: 2026-09-09 (수)
- 전제: 개인 개발자 1명, Claude Code 웹/CLI 병용, 기존 프로젝트 있음
- 원칙: **각 주차는 "증거"로 완료 판정한다.** "했다"가 아니라 명령 출력으로 확인한다.

---

## 0. 마감일이 박힌 항목 (다른 것보다 먼저)

| 마감 | 항목 | 왜 |
|---|---|---|
| **2026-08-14 (D-3)** | permission mode 명시 고정 | 이날부터 Pro/Max/Team 신규 세션 기본값이 `auto`로 전환. 직접 설정해 두면 유지됨 |
| **2026-08-31 (D-20)** | 토큰 예산 재산정 | Sonnet 5 단가가 $2/$10 → $3/$15 로 +50% |
| 즉시 | 하드코딩된 모델 ID 치환 | `claude-opus-4-1-*`(2026-08-05 은퇴), `claude-sonnet-4-*`/`claude-opus-4-*`(2026-06-15 은퇴)는 이미 실패함 |

---

## 1주차 (08-11 ~ 08-17) — 바닥 다지기: 강제 레이어부터

**이번 주 목표: "부탁"이 아니라 "강제"되는 것부터 세운다.**

| # | 작업 | 소요 | 완료 증거 (이 출력을 남긴다) |
|---|---|---|---|
| 1 | permission mode 고정 | 10분 | `/config` 출력 캡처 또는 `settings.json` 의 `defaultMode` 값 |
| 2 | 하드코딩 모델 ID 전수 검색 | 20분 | `grep -rn "claude-opus-4-1\|claude-sonnet-4-2025\|claude-opus-4-2025" .` 출력 (0건이면 0건임을 보임) |
| 3 | `starter-kit/.claude/settings.json` 의 `permissions.deny` 를 내 프로젝트에 적용 | 30분 | `.claude/settings.json` diff |
| 4 | `guard-bash.sh` / `guard-secrets.sh` 배치 + 실행권한 | 30분 | `ls -l .claude/hooks/` 출력 |
| 5 | **가드가 실제로 차단하는지 트리거 테스트** | 30분 | Claude에게 `cat .env` 를 시켜보고 차단 메시지가 뜨는 화면. **설정만 하고 테스트 안 하면 이 주차는 미완이다** |
| 6 | `/hooks` 로 등록 확인 | 5분 | `/hooks` 출력 |

**이번 주 하지 말 것**: 스킬 설치. 플러그인 탐색. 강제 레이어가 없는 상태에서 편의 기능부터 늘리면 사고 시 막을 방법이 없다.

**주말 판정**: 위 6개 중 5번(트리거 테스트)이 통과하지 않으면 2주차로 넘어가지 않는다.

---

## 2주차 (08-18 ~ 08-24) — 컨텍스트 관리

**이번 주 목표: 무엇이 컨텍스트를 먹는지 측정하고 줄인다.**

| # | 작업 | 소요 | 완료 증거 |
|---|---|---|---|
| 1 | `/context` 로 **기준선 측정**·기록 | 10분 | 항목별 토큰 수 표 (이후 모든 비교의 기준) |
| 2 | `/init` → CLAUDE.md 생성 후 **200줄 이하로 가지치기** | 1시간 | `wc -l CLAUDE.md` 출력 |
| 3 | CLAUDE.md 각 줄에 *"이 줄을 지우면 Claude가 실수하는가?"* 적용 | 1시간 | 삭제 전/후 줄 수 비교 |
| 4 | `starter-kit/CLAUDE.md` 의 §4.1(증거 기반 완료 보고) / §4.2(미확인 표기) 를 내 CLAUDE.md에 이식 | 20분 | diff |
| 5 | `/context` 재측정 | 10분 | 1번 대비 증감 |
| 6 | 서브에이전트 기본 모델을 Sonnet 5로 지정하는 습관화 | — | 실제 위임 1건의 토큰 소비 기록 |

**정량 목표**: CLAUDE.md 200줄 이하. `/context` 의 Memory files 항목이 기준선 대비 증가하지 않을 것.

**트레이드오프 경고**: CLAUDE.md를 줄이면 Claude가 프로젝트 관례를 덜 알게 된다. 줄인 만큼 3주차에서 Skill로 옮겨야 한다. 그냥 지우는 게 목표가 아니다.

---

## 3주차 (08-25 ~ 08-31) — 온디맨드 지식과 검증 루프

**이번 주 목표: 컨텍스트를 늘리지 않으면서 지식을 늘린다. + 검증을 자동화한다.**

| # | 작업 | 소요 | 완료 증거 |
|---|---|---|---|
| 1 | 2주차에서 CLAUDE.md에서 뺀 내용 중 도메인 지식을 Skill로 이전 | 2시간 | `.claude/skills/*/SKILL.md` 파일 목록 + `/context` 증가량 |
| 2 | `source-verified-research` 스킬 배치 | 10분 | `ls .claude/skills/source-verified-research/` |
| 3 | 사이드이펙트 있는 워크플로에 `disable-model-invocation: true` 적용 | 30분 | SKILL.md frontmatter |
| 4 | `verify-gate.sh` 를 내 프로젝트 테스트 명령에 맞게 수정 | 1시간 | 수정된 스크립트 + `bash -n` 통과 출력 |
| 5 | **Stop 게이트 트리거 테스트** — 일부러 테스트를 깨고 Claude가 종료 못 하는지 확인 | 30분 | 차단 메시지 화면 |
| 6 | **08-31 마감**: Sonnet 5 단가 인상 대비 예산 재산정 | 30분 | 월 예상 토큰 × 신단가 계산표 |

**Stop 게이트의 한계를 알고 쓸 것**: 8회 연속 차단하면 harness가 훅을 무시하고 턴을 끝낸다(공식 문서 명시). 따라서 **Claude가 스스로 고칠 수 있는 실패에만** 걸어야 한다. 환경 문제·미구현 기능을 걸면 8턴 낭비 후 그냥 통과한다. `verify-gate.sh` 는 3회에서 스스로 해제하도록 작성되어 있다.

---

## 4주차 (09-01 ~ 09-09) — 위임과 반증

**이번 주 목표: 작성자와 검증자를 분리한다.**

| # | 작업 | 소요 | 완료 증거 |
|---|---|---|---|
| 1 | `source-researcher.md` / `red-team-verifier.md` 서브에이전트 배치 | 20분 | `ls .claude/agents/` |
| 2 | 실제 작업 1건을 리서치 에이전트에 위임 | 1시간 | 위임 전/후 `/context` 비교 (메인 컨텍스트 증가분이 요약 크기 수준인지) |
| 3 | 그 결과를 `red-team-verifier` 에 태워 반증 | 30분 | 레드팀이 찾은 오류 목록 |
| 4 | **레드팀 지적을 1차 출처로 재확인** | 1시간 | 확인된 것 / 기각된 것 분리 기록. *(본 리서치에서 레드팀 지적 3건 중 2건 확정, 1건 기각됨)* |
| 5 | 서브에이전트 `tools` 를 최소 권한으로 축소 | 30분 | frontmatter diff |
| 6 | 큰 변경 1건에 Plan Mode 적용 | — | 승인 전 편집 0건 확인 |
| 7 | 30일 회고 | 1시간 | 1주차 기준선 vs 현재 `/context` 비교표, 실제로 트리거된 스킬 목록 |

**4주차 정리 규칙**: 4주 동안 **한 번도 트리거되지 않은 스킬은 제거한다.** 설치 개수는 그 자체로 상시 컨텍스트 비용이다.

---

## 2. 안 할 것 목록 (30일 안에 손대지 않는다)

| 항목 | 이유 |
|---|---|
| 커뮤니티 스킬 대량 설치 | 채택도 지표를 신뢰할 수 없다(02 §0). 공식 번들만으로 시작 |
| MCP 서버 다수 연결 | CLI(`gh`, `aws`)로 되는 것부터. MCP는 CLI가 없는 경우만 |
| 플러그인 자작·배포 | 개인 워크플로엔 standalone `.claude/` 로 충분 |
| Agent teams / 자체 호스팅 러너 | 개인 1인에겐 운영 비용이 이득보다 큼 |
| 하네스 3계층 프레임을 문서에 정식 도입 | 1차 출처 없음(04 §4). 내부 정리용으로만 |

---

## 3. 30일 후 판정 지표 (정량)

| 지표 | 기준선(1주차) | 30일 목표 | 측정 방법 |
|---|---|---|---|
| CLAUDE.md 줄 수 | 측정 필요 | ≤ 200 | `wc -l CLAUDE.md` |
| `/context` 상시 점유 토큰 | 측정 필요 | 기준선 이하 | `/context` |
| PreToolUse 가드 차단 실적 | 0 | ≥ 1회 트리거 테스트 통과 | 차단 화면 |
| Stop 게이트 차단 실적 | 0 | ≥ 1회 트리거 테스트 통과 | 차단 화면 |
| 서브에이전트 위임 건수 | 0 | ≥ 3건 | 세션 기록 |
| 레드팀 검증을 거친 산출물 | 0 | ≥ 1건 | 반증 목록 |
| 미사용 스킬 | — | 0개 | 4주차 정리 |

---

## 4. 이 로드맵의 위험

| 위험 | 대응 |
|---|---|
| 1주차 훅 스크립트가 오작동해 모든 작업이 차단됨 | 가드는 fail-closed(jq 없으면 차단)로 설계됨. 문제 시 `.claude/settings.json` 의 `hooks` 블록을 통째로 주석 처리하고 원인 파악 |
| Stop 게이트가 무한 차단 루프 | `verify-gate.sh` 가 3회에서 자동 해제. harness도 8회에서 해제 |
| 2주차에 CLAUDE.md를 과하게 잘라 품질 저하 | 3주차 Skill 이전이 짝이다. 자르기만 하고 끝내지 말 것 |
| 2026-08-14 auto mode 전환을 놓침 | 1주차 1번 항목이 이것. 최우선 |
| 커뮤니티 스킬 도입 판단을 star로 함 | 02 §5의 6단계 절차 사용 |
